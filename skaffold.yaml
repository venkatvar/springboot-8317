apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/venkatvar/springboot-8317:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/venkatvar/springboot-8317:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: springboot-8317
        chartPath: charts/springboot-8317
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/venkatvar/springboot-8317"
          image.tag: "{{.DIGEST_HEX}}"
